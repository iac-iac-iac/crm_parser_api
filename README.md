# DataMaster Phone Collector 📱

**Профессиональная система сбора телефонных данных из CRM DataMaster**

## 🎯 Описание проекта

DataMaster Phone Collector - это мощное Python-приложение с графическим интерфейсом для автоматизированного сбора и экспорта телефонных номеров из CRM-системы DataMaster через их официальный API.

### Ключевые возможности

✨ **Графический интерфейс** - Удобный GUI на базе CustomTkinter  
✨ **Многопоточность** - Параллельная обработка запросов  
✨ **Умное управление состоянием** - Автоматическое сохранение и восстановление прогресса  
✨ **Rate Limiting** - Контроль частоты запросов к API  
✨ **Экспорт в CSV** - Структурированное сохранение данных  
✨ **Логирование** - Детальные логи работы приложения  
✨ **Нормализация данных** - Автоматическая очистка и форматирование номеров

## 📋 Требования

- Python 3.8+
- Доступ к API DataMaster
- Токен авторизации DataMaster

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/iac-iac-iac/crm_parser_api.git
cd crm_parser_api
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка окружения

Создайте файл `.env` в корневой директории проекта:

```env
DATAMASTER_API_URL=https://api.datamaster.com
DATAMASTER_API_TOKEN=your_token_here
DATABASE_PATH=data/phones.db
RATE_LIMIT_DELAY=0.5
```

## 💻 Использование

### Запуск GUI приложения

```bash
python main.py
```

### Запуск с консольным интерфейсом

```bash
python gui_main.py
```

## 🏗️ Архитектура проекта

```
crm_parser_api/
├── src/
│   ├── api/              # API клиент
│   │   ├── client.py     # DataMaster API клиент
│   │   └── __pycache__/
│   ├── collector/        # Логика сбора данных
│   │   ├── normalizer.py      # Нормализация телефонов
│   │   ├── orchestrator.py    # Оркестратор сбора
│   │   ├── progress_orchestrator.py  # Управление прогрессом
│   │   └── state_manager.py   # Управление состоянием
│   ├── database/         # Работа с БД
│   │   └── manager.py    # Менеджер базы данных
│   ├── gui/              # Графический интерфейс
│   │   └── app.py        # GUI приложение
│   ├── reports/          # Экспорт данных
│   │   └── exporter.py   # CSV экспортер
│   └── utils/            # Утилиты
│       └── retry.py      # Retry декоратор
├── data/
│   └── exports/          # Экспортированные файлы
├── logs/                 # Логи приложения
├── main.py              # Точка входа с GUI
├── gui_main.py          # Альтернативная точка входа
├── phones.db            # SQLite база данных
└── requirements.txt     # Зависимости проекта
```

## 🔧 Основные компоненты

### API Client (`src/api/client.py`)

Клиент для работы с API DataMaster:
- Авторизация через токен
- Получение списка клиентов и проектов
- Извлечение телефонных номеров
- Обработка ошибок и повторные попытки

### Collection Orchestrator (`src/collector/orchestrator.py`)

Координирует процесс сбора данных:
- Многопоточная обработка
- Управление очередями
- Rate limiting
- Обратные вызовы для прогресса

### State Manager (`src/collector/state_manager.py`)

Управление состоянием приложения:
- Сохранение прогресса
- Восстановление после прерываний
- Отслеживание обработанных элементов

### Phone Normalizer (`src/collector/normalizer.py`)

Нормализация телефонных номеров:
- Удаление лишних символов
- Унификация форматов
- Валидация номеров

### Database Manager (`src/database/manager.py`)

Работа с SQLite базой данных:
- Создание схемы
- Сохранение телефонов
- Проверка дубликатов

### CSV Exporter (`src/reports/exporter.py`)

Экспорт данных:
- Генерация CSV файлов
- Форматирование данных
- Временные метки

### GUI Application (`src/gui/app.py`)

Графический интерфейс:
- Настройка параметров
- Отображение прогресса
- Управление процессом
- Логирование в реальном времени

## 📊 Формат данных

### CSV файл экспорта

```csv
phone,first_seen_at,original_format,first_run_id
+79161234567,2024-01-15 10:30:00,+7 (916) 123-45-67,run_20240115_103000
```

### База данных SQLite

**Таблица phones:**
- `phone` (TEXT PRIMARY KEY) - нормализованный номер
- `created_at` (TEXT) - дата первого обнаружения
- `original_format` (TEXT) - исходный формат
- `first_run_id` (TEXT) - ID первого запуска

**Таблица collection_state:**
- `id` (INTEGER PRIMARY KEY)
- `state_data` (TEXT) - JSON с состоянием
- `updated_at` (TEXT) - время обновления

## ⚙️ Конфигурация

### Переменные окружения (.env)

| Переменная | Описание | Значение по умолчанию |
|-----------|----------|----------------------|
| `DATAMASTER_API_URL` | URL API DataMaster | - |
| `DATAMASTER_API_TOKEN` | Токен авторизации | - |
| `DATABASE_PATH` | Путь к БД | `data/phones.db` |
| `RATE_LIMIT_DELAY` | Задержка между запросами (сек) | `0.5` |

### Параметры приложения

- **Лимит клиентов**: Максимальное количество клиентов для обработки
- **Лимит проектов**: Максимальное количество проектов на клиента
- **Max pages**: Максимальное количество страниц телефонов

## 🐛 Отладка

### Логи

Логи сохраняются в папку `logs/`:
- `collector_YYYYMMDD.log` - логи работы коллектора
- Уровень логирования: INFO

### Состояние

Состояние сохраняется автоматически в БД. Для сброса:

```python
from src.database.manager import DatabaseManager

db = DatabaseManager('data/phones.db')
db.clear_state()
```

## 🔒 Безопасность

- ⚠️ Никогда не коммитьте файл `.env` в репозиторий
- 🔑 Храните токены в безопасности
- 🛡️ Используйте переменные окружения для чувствительных данных

## 📝 Лицензия

Проект создан для внутреннего использования.

## 👨‍💻 Автор

Создано для работы с DataMaster CRM API.

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи в папке `logs/`
2. Убедитесь в корректности `.env` файла
3. Проверьте доступность API DataMaster
4. Проверьте валидность токена

## 📈 Roadmap

- [ ] Поддержка других форматов экспорта (Excel, JSON)
- [ ] Web-интерфейс
- [ ] API для интеграции
- [ ] Расширенная аналитика
- [ ] Фильтрация по регионам

---

**Версия**: 1.0.0  
**Последнее обновление**: 2024
